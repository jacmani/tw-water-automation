import CommitteeAdmin from '@/components/committee/CommitteeAdmin';

export const metadata = { title: 'Committee Admin — Trinity World Water' };

export default function AdminPage() {
  return <CommitteeAdmin />;
}
