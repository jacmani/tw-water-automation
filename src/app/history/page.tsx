import HistoryPage from '@/components/history/HistoryPage';

export const metadata = { title: 'Consumption History — Trinity World Water Consumption' };

export default function Page() {
  return <HistoryPage />;
}
